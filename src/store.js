import createStore from '@ducharmemp/mobstr';

export const {
    addOne,
    findAll,
    primaryKey,
    relationship,
    removeOne,
    truncateCollection,
} = createStore();
console.log(createStore())
